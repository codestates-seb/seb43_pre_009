server:
  port: 8080

# database 연동 설정
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    #    TODO : 데이터베이스 연동 해야합니다.
    url: jdbc:mysql://localhost:3306/database-2?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: admin
    password: suzka5-kidsih-kexdaC
  thymeleaf:
    cache: false

  # spring data jpa 설정
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update
